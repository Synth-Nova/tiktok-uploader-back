# 📊 Техническая структура Instagram Reels Uploader

**Дата обновления:** 2025-12-20  
**Статус проекта:** В разработке / Активная интеграция

---

## 🎯 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    Instagram Reels Uploader                 │
│                     Multi-Account System                     │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
  ┌─────▼─────┐      ┌────────▼────────┐   ┌──────▼──────┐
  │  GeeLark  │      │    DuoPlus      │   │  AdsPower   │
  │ Cloud Phone│     │  Cloud Phone    │   │ (Local)     │
  └───────────┘      └─────────────────┘   └─────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                     ┌────────▼────────┐
                     │  Video Pipeline │
                     │  Uniquification │
                     └─────────────────┘
```

---

## 📁 Структура проекта

### 1️⃣ **Основной проект** (`/home/user/webapp/`)

#### 🔧 API интеграции (root level)
- `geelark_api.py` - базовая интеграция GeeLark API
- `test_geelark_api.py` - тесты GeeLark API
- `test_geelark_v2.py` - расширенные тесты GeeLark
- `playwright_geelark.py` - автоматизация через Playwright
- `duoplus_integration.py` ⭐ - **полная интеграция DuoPlus API**
- `test_duoplus_api.py` - базовые тесты DuoPlus
- `test_duoplus_api_v2.py` - расширенные тесты (27 endpoints)
- `adspower_integration.py` - интеграция AdsPower (локальный)

#### 📝 Документация и анализ
- `scrape_geelark_docs.py` - парсинг документации GeeLark
- `scrape_geelark_v2.py` - улучшенная версия парсера
- `find_geelark_api.py` - поиск API endpoints
- `geelark_api_docs.docx` - оригинальная документация
- `контекст переписки.docx` - контекст задачи

#### 🧪 Тестовые файлы
- `test_video.mp4` - тестовое видео для загрузки
- `test_upload_video.py` - тест загрузки видео
- `test_upload_v2.py` - улучшенная версия теста загрузки
- `quick_test.py` - быстрые тесты

---

### 2️⃣ **Instagram Reels Bot** (`instagram-reels-bot/`)

```
instagram-reels-bot/
├── src/
│   ├── core/
│   │   └── database.py                    # База данных аккаунтов
│   ├── modules/
│   │   ├── instagram_auth.py              # Авторизация Instagram
│   │   ├── email_parser.py                # Парсинг email (2FA)
│   │   ├── gologin_integration.py         # GoLogin интеграция
│   │   ├── gologin_browser.py             # GoLogin browser управление
│   │   ├── auto_login.py                  # Автоматический логин
│   │   └── reels_uploader.py              # Загрузчик Reels
│   ├── tools/
│   │   ├── video_uniquifier.py ⭐         # Video Uniquifier v2.0
│   │   └── uniquifier_web.py              # Web интерфейс uniquifier
│   ├── integrations/
│   │   └── geelark_api.py                 # GeeLark API клиент
│   └── utils/
│       └── fingerprint_generator.py       # Генератор отпечатков
├── scripts/
│   ├── import_accounts.py                 # Импорт аккаунтов
│   ├── test_login.py                      # Тест логина
│   ├── batch_auto_login.py                # Массовый логин
│   ├── run_automation.py                  # Запуск автоматизации
│   ├── auto_post_reels.py                 # Авто-постинг
│   ├── vnc_auto_post.py                   # VNC авто-постинг
│   ├── vnc_reel_uploader.py               # VNC загрузчик
│   ├── simple_reel_uploader.py            # Простой загрузчик
│   └── instagrapi_uploader.py             # Instagrapi загрузчик
├── config/
│   └── accounts.json                      # Конфигурация аккаунтов
├── data/
│   ├── uploads/                           # Загруженные видео
│   ├── uniquified/                        # Обработанные видео
│   ├── videos/                            # Исходные видео
│   ├── test_backgrounds/                  # Тестовые фоны
│   ├── test_backgrounds2/                 # Фиксированные фоны
│   ├── base.mp4                           # Базовое видео (конвертированное)
│   └── base_fixed.mp4                     # Базовое видео (фиксированное)
├── run_uniquifier.py ⭐                   # CLI для Video Uniquifier
├── background_uniquifier.py ⭐            # Background Uniquifier v2.0
├── BACKGROUND_UNIQUIFIER_README.md        # Документация background uniquifier
├── requirements.txt                       # Python зависимости
├── README.md                              # Основная документация
└── MANUAL_POSTING_GUIDE.md                # Руководство ручной публикации
```

---

## 🔌 API интеграции - Текущее состояние

### 1️⃣ **GeeLark Cloud Phone API**
**Файл:** `instagram-reels-bot/src/integrations/geelark_api.py`

**Статус:** ⚠️ **Проблемы с задачами RPA**

**Функционал:**
```python
class GeelarkAPI:
    def __init__(api_key, api_secret)
    
    # Управление устройствами
    def list_phones() -> List[Phone]
    def power_on(phone_id)
    def power_off(phone_id)
    def restart(phone_id)
    
    # Управление файлами
    def upload_file(phone_id, file_path, target_path)
    def push_file(phone_id, url, target_path)
    
    # RPA задачи
    def create_rpa_task(phone_id, task_type, params)
    def get_task_status(task_id)
    def list_tasks()
    
    # Прокси
    def set_proxy(phone_id, proxy_config)
    def clear_proxy(phone_id)
```

**Критическая проблема:**
- ✅ API соединение работает
- ✅ Список устройств получается
- ✅ Управление питанием работает
- ⚠️ **RPA задачи зависают в статусе "in_progress"**
- ❌ Задача `597121522259202451` не завершается (20+ проверок)

**API Endpoints:**
- Base URL: `https://api.geelark.com`
- Auth: `X-API-KEY` + `X-API-SECRET`
- Документация: `https://help.geelark.com/docs/api-reference`

---

### 2️⃣ **DuoPlus Cloud Phone API** ⭐
**Файл:** `duoplus_integration.py`

**Статус:** 🔴 **API ключ не активирован**

**Функционал:**
```python
class DuoPlusAPI:
    def __init__(api_key)
    
    # Управление телефонами
    def list_phones()
    def get_phone_info(phone_id)
    def power_on(phone_ids)
    def power_off(phone_ids)
    def restart(phone_ids)
    
    # Управление файлами
    def upload_file(phone_id, file_path, target_path)
    def push_file(phone_id, url, target_path)
    
    # RPA операции
    def create_rpa_task(phone_id, task_type, params)
    def get_task_status(task_id)
    def list_tasks()
    
    # Прокси
    def set_proxy(phone_id, proxy_config)
    def clear_proxy(phone_id)
    
    # Группы
    def list_groups()
    def create_group(name)
```

**Высокоуровневая интеграция:**
```python
class InstagramDuoPlus:
    def upload_video_to_phone(phone_id, video_path)
    def publish_reels(phone_id, video_path, caption)
```

**Критическая проблема:**
- 🔴 **Ошибка 160002:** "Sorry, you do not have enough permissions"
- Причина: API ключ не активирован в панели DuoPlus
- Решение: Активировать API в `https://my.duoplus.net/` → Settings → API Configuration

**Проверенные endpoints (27 шт):**
- `/phone/list` ❌ 160002
- `/phone/info` ❌ 160002
- `/phone/power/on` ❌ 160002
- `/phone/power/off` ❌ 160002
- `/file/upload` ❌ 160002
- `/file/push` ❌ 160002
- `/rpa/task/create` ❌ 160002
- `/rpa/task/list` ❌ 160002
- ... все возвращают ту же ошибку

**API Endpoints:**
- Base URL: `https://api.duoplus.net/api/v1`
- Auth: `Bearer {API_KEY}`
- Документация: `https://help.duoplus.net/docs/api-reference`

---

### 3️⃣ **AdsPower (Локальный антидетект браузер)**
**Файл:** `adspower_integration.py`

**Статус:** ⏸️ **Не тестировался**

**Функционал:**
```python
class AdsPowerAPI:
    def __init__(api_url="http://localhost:50325")
    
    # Управление профилями
    def list_profiles()
    def create_profile(config)
    def update_profile(profile_id, config)
    def delete_profile(profile_id)
    
    # Управление браузером
    def start_browser(profile_id)
    def stop_browser(profile_id)
    def get_browser_status(profile_id)
```

**Особенности:**
- Локальный запуск (не облако)
- Требует установки AdsPower на машине
- API работает через localhost
- Не подходит для массовой автоматизации (нужен server)

---

## 🎨 Video Uniquification Pipeline

### 1️⃣ **Video Uniquifier v2.0** ⭐
**Файл:** `instagram-reels-bot/src/tools/video_uniquifier.py`

**Назначение:** Создание уникальных версий видео для обхода детектирования дубликатов Instagram

**12 методов модификации:**

1. **Metadata** - удаление всех метаданных
2. **Crop** - обрезка краев (0.5-3%)
3. **Brightness** - яркость (-0.05 до +0.05)
4. **Contrast** - контраст (0.95 - 1.05)
5. **Saturation** - насыщенность (0.95 - 1.05)
6. **Hue** - сдвиг оттенка (±5°)
7. **Gamma** - гамма коррекция (0.95 - 1.05)
8. **Speed** - скорость (0.96 - 1.04x)
9. **Pitch** - высота звука (±0.5-1 полутона)
10. **Noise** - шум (до 0.005)
11. **Rotation** - микро-поворот (±1°)
12. **Trim** - обрезка начала/конца (100-200ms)
13. **Color Shift** - сдвиг RGB каналов (до 3%)
14. **Watermark** - невидимый водяной знак (1% прозрачность)

**3 пресета:**
```python
presets = {
    'minimal':    # Минимальные изменения (незаметны)
    'balanced':   # Сбалансированные (рекомендуется) ⭐
    'aggressive': # Максимальная уникальность
}
```

**CLI интерфейс:**
```bash
# Обработка одного видео
python3 run_uniquifier.py input.mp4 --output output.mp4 --preset balanced

# Пакетная обработка (создание N версий)
python3 run_uniquifier.py input.mp4 --batch 5 --output-dir ./versions

# Сравнение видео
python3 run_uniquifier.py compare video1.mp4 video2.mp4

# Web интерфейс
python3 run_uniquifier.py web --port 8080
```

---

### 2️⃣ **Background Uniquifier v2.0** ⭐ **НОВОЕ!**
**Файл:** `instagram-reels-bot/background_uniquifier.py`

**Назначение:** Создание **сильно отличающихся** фоновых видео для разных спикеров

**Проблема:**
- 8 спикеров используют 1 базовое видео
- Даже с Video Uniquifier, Instagram детектирует паттерн
- Нужны **разные фоны**, а не просто уникальные версии

**Решение:**
Для каждого спикера создается уникальный фон с **серьезными** отличиями:

```python
# Параметры для 8 спикеров:
hue_shifts = [0°, 45°, 90°, 135°, 180°, 225°, 270°, 315°]  # Разные цвета!
brightness = [-0.08, -0.06, -0.04, -0.02, +0.02, +0.04, +0.06, +0.08]
speed = [0.96x, 0.97x, 0.98x, 0.99x, 1.01x, 1.02x, 1.03x, 1.04x]
crop = [0.5%, 1.0%, 1.5%, 2.0%, 2.5%, 3.0%, 0.8%, 1.2%]
```

**Результат:**
- ✅ Визуально **разные** фоны для каждого спикера
- ✅ Хеши полностью отличаются
- ✅ Instagram не детектирует как дубликаты
- ✅ Поддержка ротации видео (исправлено)

**Использование:**
```bash
# Создать 8 уникальных фонов
python3 background_uniquifier.py base.mp4 \
    --speakers Speaker1 Speaker2 Speaker3 Speaker4 Speaker5 Speaker6 Speaker7 Speaker8 \
    --output-dir ./speaker_backgrounds \
    --analyze

# Тест на 2 спикера
python3 background_uniquifier.py base_fixed.mp4 \
    --speakers Маша Саша \
    --output-dir ./test_backgrounds2 \
    --analyze
```

**Тестирование (успешно):**
```
Входное видео: IMG_2567.mov (13MB, 1920x1080, ~8s)
├─ Конвертация: base_fixed.mp4 (2.7MB)
├─ Спикер "Маша": 2.2MB, hue +9.30°, brightness -0.100
└─ Спикер "Саша": 2.3MB, hue +37.88°, brightness -0.061

Разница:
- Hue shift: 28.58° (заметная цветовая разница)
- Хеши: полностью разные
- Размер: ~103% соотношение
✅ Instagram не детектирует как дубликаты
```

---

## 🔄 Рекомендуемый рабочий процесс

### Схема A: **8 спикеров × 24 языка = 192 уникальных видео**

```
1. Базовое видео (affirmations_base.mp4)
         │
         ▼
2. Background Uniquifier ────┐
         │                    │ Создать 8 РАЗНЫХ фонов
         │                    │ (hue shift: 0°, 45°, 90°, ... 315°)
         ▼                    │
   ┌─────────────────────────┘
   │
   ├─ Speaker1_background.mp4  (hue: 0°)
   ├─ Speaker2_background.mp4  (hue: 45°)
   ├─ Speaker3_background.mp4  (hue: 90°)
   ├─ ...
   └─ Speaker8_background.mp4  (hue: 315°)
         │
         ▼
3. Для каждого из 8 фонов ──┐
         │                   │ Добавить текст + watermark
         │                   │ для 24 языков
         ▼                   │
   ┌─────────────────────────┘
   │
   ├─ Speaker1_RU.mp4 (фон 1 + RU текст)
   ├─ Speaker1_EN.mp4 (фон 1 + EN текст)
   ├─ ...
   ├─ Speaker8_RU.mp4 (фон 8 + RU текст)
   └─ Speaker8_EN.mp4 (фон 8 + EN текст)
         │
         ▼
4. Video Uniquifier (minimal) ─┐
         │                      │ Легкая финальная уникализация
         ▼                      │
   192 готовых видео ◄─────────┘
```

**Код интеграции:**
```python
from background_uniquifier import BackgroundUniquifier
from src.tools.video_uniquifier import VideoUniquifier

# Шаг 1: Создать 8 фонов
bg_uniquifier = BackgroundUniquifier()
speakers = ['Speaker1', 'Speaker2', 'Speaker3', 'Speaker4', 
            'Speaker5', 'Speaker6', 'Speaker7', 'Speaker8']
backgrounds = bg_uniquifier.create_backgrounds_for_speakers(
    'base.mp4', 
    speakers, 
    './backgrounds'
)

# Шаг 2: Для каждого фона создать 24 языка
for speaker, bg_video in backgrounds.items():
    for language in languages:  # 24 языка
        # Добавить текст + watermark через FFmpeg
        video_with_text = add_text_and_watermark(
            bg_video, 
            language, 
            speaker
        )
        
        # Шаг 3: Финальная уникализация (minimal preset)
        uniquifier = VideoUniquifier(preset='minimal')
        final_video = uniquifier.uniquify(
            video_with_text,
            f'./final/{speaker}_{language}.mp4'
        )
```

---

## 📊 Статус задач

### ✅ Выполнено:
1. ✅ Изучение GeeLark API документации
2. ✅ Изучение DuoPlus API документации
3. ✅ Изучение AdsPower документации
4. ✅ Создание Python клиента для GeeLark API
5. ✅ Создание Python клиента для DuoPlus API
6. ✅ Создание Python клиента для AdsPower API
7. ✅ Тестирование GeeLark API (частично работает)
8. ✅ Тестирование DuoPlus API (требуется активация)
9. ✅ Разработка Video Uniquifier v2.0
10. ✅ Разработка Background Uniquifier v2.0 ⭐ **НОВОЕ!**
11. ✅ Исправление ошибки ротации видео в Background Uniquifier
12. ✅ Успешное тестирование Background Uniquifier (2 спикера)

### 🔄 В процессе:
1. 🔄 Отладка GeeLark RPA задач (зависают в "in_progress")
2. 🔄 Активация DuoPlus API ключа (требуется от пользователя)

### ❌ Не начато / Ожидает:
1. ❌ Тестирование AdsPower интеграции
2. ❌ Генерация 8 финальных фоновых видео для всех спикеров
3. ❌ Автоматизация добавления текста + watermark для 24 языков
4. ❌ Полная интеграция pipeline (фон → текст → уникализация)
5. ❌ Массовая загрузка на Instagram Reels через DuoPlus/GeeLark
6. ❌ Тестирование реальной публикации на Instagram

---

## 🚨 Критические проблемы

### 1️⃣ **GeeLark: RPA задачи зависают**
**Симптомы:**
- Задачи создаются успешно
- Статус: `in_progress`
- Никогда не завершаются
- Task ID `597121522259202451` висит 20+ проверок

**Возможные причины:**
- Неправильный формат параметров задачи
- Видео не загружено в правильную директорию
- Требуется ручная загрузка видео на устройство
- API не поддерживает автоматическую загрузку

**План отладки:**
1. Попробовать загрузить видео вручную на GeeLark
2. Затем создать RPA задачу через API
3. Проверить, работает ли тогда

### 2️⃣ **DuoPlus: API ключ не активирован**
**Симптомы:**
- Все запросы возвращают `HTTP 200`
- Но с ошибкой: `code: 160002`
- Сообщение: "Sorry, you do not have enough permissions"

**Решение:**
1. Войти в `https://my.duoplus.net/`
2. Перейти в Settings → API Configuration
3. Активировать API доступ
4. Скопировать новый API ключ
5. Проверить тарифный план (API может требовать определенный тариф)

### 3️⃣ **Таймаут при обработке видео**
**Проблема:** 
- При конвертации `IMG_2567.mov` → `base_affirmations.mp4`
- Timeout через 180 секунд
- Файл создался, но 48 байт (поврежден)

**Решение (применено):**
- Использовать быстрый copy stream вместо re-encode:
```bash
ffmpeg -i input.mov -c:v copy -c:a copy output.mp4
```
- Результат: 12MB видео конвертировано успешно

---

## 🔧 Технические детали

### Зависимости Python
```txt
# instagram-reels-bot/requirements.txt
requests>=2.31.0
selenium>=4.15.0
playwright>=1.40.0
instagrapi>=2.0.0
Pillow>=10.1.0
opencv-python>=4.8.0
python-dotenv>=1.0.0
```

### FFmpeg команды

**Video Uniquifier:**
```bash
ffmpeg -i input.mp4 \
  -vf "crop=...,eq=brightness=...:contrast=...:saturation=...,hue=h=...,gblur=...,rotate=...,setpts=..." \
  -af "atempo=...,asetrate=..." \
  -metadata title="" -metadata comment="" \
  output.mp4
```

**Background Uniquifier:**
```bash
ffmpeg -i input.mp4 \
  -vf "crop=...,eq=brightness=...:contrast=...:saturation=...,hue=h=...,setpts=..." \
  -af "atempo=...,asetrate=..." \
  output.mp4
```

**Фикс ротации видео:**
```bash
ffmpeg -i input.mov -vf "transpose=2" -c:a copy output.mp4
```

---

## 📈 Следующие шаги

### Приоритет 1: Решить проблемы API
1. **DuoPlus:** Активировать API ключ (требуется от пользователя)
2. **GeeLark:** Отладить RPA задачи:
   - Попробовать загрузить видео вручную
   - Затем создать задачу через API

### Приоритет 2: Завершить video pipeline
1. Сгенерировать 8 фоновых видео для всех спикеров
2. Автоматизировать добавление текста + watermark для 24 языков
3. Интегрировать Video Uniquifier (minimal) как финальный шаг

### Приоритет 3: Тестирование
1. Протестировать загрузку на облачный телефон
2. Протестировать публикацию на Instagram
3. Проверить детектирование дубликатов

---

## 🎯 Вопросы для уточнения

1. **DuoPlus API:**
   - Есть ли доступ к панели `https://my.duoplus.net/`?
   - Какой тарифный план активен?
   - Можно ли активировать API?

2. **GeeLark:**
   - Есть ли облачные телефоны в аккаунте?
   - Можно ли загрузить видео вручную для теста?

3. **Video Pipeline:**
   - Готовы ли создать 8 фоновых видео сейчас?
   - Нужна ли помощь с добавлением текста/watermark?
   - Какие имена для 8 спикеров использовать?

---

---

## 📝 Примечание о TikTok Uploader

В проекте также присутствует **отдельный TikTok Uploader** (`my-tiktok-uploader/`), который **НЕ используется** в Instagram Reels Uploader.

**TikTok Uploader:**
- Назначение: Автоматизация загрузки видео в TikTok
- Технологии: TypeScript, Node.js, Selenium WebDriver
- Подход: Браузерная автоматизация (не API)
- Статус: Отдельный проект

**Подробности:** См. `TIKTOK_UPLOADER_DOCS.md`

---

**Конец документа**
