# 🎬 Интеграция Video Uniquifier в Форк проекта

**Дата:** 2025-12-22  
**Проект:** Influence Dev (ФОРК) - Timeweb ID 6186087  
**Основной проект:** Timeweb ID 5788751 (НЕ ТРОГАЕМ!)

---

## 📋 Задача

Добавить полностью работающий Uniquifier в меню React frontend **ТОЛЬКО в форке** (ID 6186087).

---

## ✅ Текущий статус

### Что уже есть:
1. ✅ **Frontend страница Uniquifier** - `/uniquifier` (в меню, строка 26 Layout.tsx)
2. ✅ **Python Backend** - `instagram-reels-bot/src/tools/uniquifier_web.py`
3. ✅ **Маршрут в App.tsx** - строка 94-102
4. ✅ **UI компонент** - полностью готов

### Что НЕ работает:
1. ❌ Python backend **НЕ запущен** на production сервере (217.198.12.144:8080)
2. ❌ Frontend **НЕ подключен** к Python backend API
3. ❌ Переменная окружения `REACT_APP_UNIQUIFIER_URL` не настроена

---

## 🚀 План интеграции

### Phase 1: Настройка Backend на Timeweb Cloud

#### 1.1 Подготовка Python окружения

```bash
# SSH на сервер Timeweb
ssh user@217.198.12.144

# Перейти в директорию проекта
cd /path/to/project/instagram-reels-bot

# Установить зависимости (если нужно)
pip3 install flask

# Проверить FFmpeg
ffmpeg -version
```

#### 1.2 Запуск Uniquifier API

```bash
# Запустить в фоне с помощью nohup
nohup python3 run_uniquifier.py web 8080 > /var/log/uniquifier.log 2>&1 &

# ИЛИ с помощью supervisor (рекомендуется)
cat > /etc/supervisor/conf.d/uniquifier.conf << 'SUPERVISOR'
[program:uniquifier]
command=python3 /path/to/instagram-reels-bot/run_uniquifier.py web 8080
directory=/path/to/instagram-reels-bot
autostart=true
autorestart=true
user=www-data
stdout_logfile=/var/log/uniquifier.log
stderr_logfile=/var/log/uniquifier_error.log
SUPERVISOR

# Перезапустить supervisor
supervisorctl reread
supervisorctl update
supervisorctl start uniquifier
```

#### 1.3 Проверка работы API

```bash
# Проверить, что сервис запущен
curl http://localhost:8080/

# Проверить API endpoint
curl http://217.198.12.144:8080/
```

#### 1.4 Настройка Nginx (если нужен proxy)

```nginx
# /etc/nginx/sites-available/default

location /api/uniquify {
    proxy_pass http://localhost:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    
    # Для больших файлов
    client_max_body_size 500M;
    proxy_read_timeout 600s;
}

location /download {
    proxy_pass http://localhost:8080;
    proxy_set_header Host $host;
}

location /download-all {
    proxy_pass http://localhost:8080;
    proxy_set_header Host $host;
}
```

```bash
# Перезапустить Nginx
nginx -t
systemctl restart nginx
```

---

### Phase 2: Настройка Frontend

#### 2.1 Обновить .env.production

```bash
# В директории my-tiktok-uploader/frontend

cat > .env.production << 'ENVFILE'
# TikTok API
REACT_APP_API_URL=http://217.198.12.144:3000

# Uniquifier API
REACT_APP_UNIQUIFIER_URL=http://217.198.12.144:8080

# YouTube API (old server)
REACT_APP_YOUTUBE_API_URL=http://72.56.76.237:3000
ENVFILE
```

#### 2.2 Пересобрать Frontend

```bash
cd /home/user/webapp/my-tiktok-uploader/frontend

# Установить зависимости (если нужно)
npm install

# Собрать production build
npm run build

# Результат в ./build/
```

#### 2.3 Деплой Frontend на Timeweb

**Вариант 1: SFTP**
```bash
# Локально (в sandbox)
cd /home/user/webapp/my-tiktok-uploader/frontend

# Создать архив
tar -czf frontend-build.tar.gz build/

# Загрузить на сервер (через SFTP или scp)
scp frontend-build.tar.gz user@217.198.12.144:/tmp/

# На сервере
ssh user@217.198.12.144
cd /var/www/html
tar -xzf /tmp/frontend-build.tar.gz
mv build/* .
rm -rf build
```

**Вариант 2: Git Push + Build на сервере**
```bash
# На сервере Timeweb
cd /path/to/my-tiktok-uploader/frontend
git pull origin main
npm install
npm run build
cp -r build/* /var/www/html/
```

---

### Phase 3: Проверка работы

#### 3.1 Проверить Backend API

```bash
# Локально
curl http://217.198.12.144:8080/

# Должно вернуть HTML страницу
```

#### 3.2 Проверить Frontend

1. Открыть: `https://217.198.12.144/uniquifier`
2. Войти: admin / rewfdsvcx5
3. Нажать на "Уникализатор" в меню
4. Загрузить тестовое видео
5. Создать уникальные версии

#### 3.3 Проверить API подключение

Открыть DevTools в браузере:
- Network tab
- Загрузить видео
- Проверить запрос к `http://217.198.12.144:8080/api/uniquify`

---

## 🔧 Troubleshooting

### Проблема 1: CORS ошибки

**Решение:** Добавить CORS в Python backend

```python
# В uniquifier_web.py, после создания app

from flask_cors import CORS
CORS(app, origins=['https://217.198.12.144'])
```

Установить flask-cors:
```bash
pip3 install flask-cors
```

### Проблема 2: API не отвечает

**Проверки:**
```bash
# Проверить процесс
ps aux | grep uniquifier

# Проверить порт
netstat -tuln | grep 8080

# Проверить логи
tail -f /var/log/uniquifier.log
```

### Проблема 3: Frontend не подключается к API

**Проверки:**
1. Проверить переменную окружения в build:
   ```bash
   grep -r "REACT_APP_UNIQUIFIER_URL" build/
   ```

2. Проверить в браузере (DevTools → Console):
   ```javascript
   console.log(process.env.REACT_APP_UNIQUIFIER_URL)
   ```

3. Пересобрать frontend с правильной переменной

---

## 📊 Архитектура после интеграции

```
┌──────────────────────────────────────────────────────────────┐
│  Timeweb Cloud ID: 6186087 (ФОРК - Influence Dev)           │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Frontend: https://217.198.12.144/                     │  │
│  │  - TikTok Upload UI                                    │  │
│  │  - YouTube Upload UI                                   │  │
│  │  - Uniquifier UI (/uniquifier) ✅ НОВОЕ!              │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  TikTok API: http://217.198.12.144:3000               │  │
│  │  - Selenium-based TikTok uploader                      │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Uniquifier API: http://217.198.12.144:8080 ✅ НОВОЕ! │  │
│  │  - Video Uniquifier v2.0 (Python/Flask)               │  │
│  │  - FFmpeg video processing                             │  │
│  │  - 12 modification methods                             │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
└───────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  External YouTube Server: http://72.56.76.237:3000           │
│  (Не меняем)                                                  │
└───────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  Timeweb Cloud ID: 5788751 (ОСНОВНОЙ ПРОЕКТ)                 │
│  ⚠️ НЕ ТРОГАЕМ! Остается без изменений                       │
└───────────────────────────────────────────────────────────────┘
```

---

## ✅ Checklist деплоя

### Backend:
- [ ] Python backend запущен на 217.198.12.144:8080
- [ ] FFmpeg установлен
- [ ] Директории созданы (`data/uploads`, `data/uniquified`)
- [ ] Сервис настроен на автостарт (supervisor/systemd)
- [ ] API отвечает на `/` и `/api/uniquify`
- [ ] CORS настроен (если нужно)

### Frontend:
- [ ] `.env.production` обновлен с `REACT_APP_UNIQUIFIER_URL`
- [ ] Frontend пересобран (`npm run build`)
- [ ] Build задеплоен на сервер
- [ ] Nginx обслуживает статику
- [ ] Uniquifier видно в меню
- [ ] Страница `/uniquifier` загружается

### Testing:
- [ ] Можно загрузить видео
- [ ] Видео обрабатывается
- [ ] Можно скачать результаты
- [ ] Нет CORS ошибок
- [ ] Нет 404 ошибок в Network tab

---

## 📚 Полезные команды

### На сервере Timeweb:
```bash
# Проверить статус Uniquifier
supervisorctl status uniquifier

# Перезапустить
supervisorctl restart uniquifier

# Посмотреть логи
tail -f /var/log/uniquifier.log

# Проверить порт
netstat -tuln | grep 8080
```

### Локально (для сборки):
```bash
cd /home/user/webapp/my-tiktok-uploader/frontend

# Пересобрать frontend
npm run build

# Создать архив для деплоя
tar -czf frontend-$(date +%Y%m%d).tar.gz build/
```

---

## 🔐 Важно!

1. **Все изменения ТОЛЬКО в форке** (ID 6186087)
2. **Основной проект** (ID 5788751) остается нетронутым
3. **Коммитить изменения** в форк после каждого шага
4. **Документировать** все изменения

---

## 📝 Следующие шаги

1. ✅ Создать этот документ
2. ⏳ Запустить Python backend на Timeweb Cloud
3. ⏳ Настроить .env.production
4. ⏳ Пересобрать и задеплоить frontend
5. ⏳ Протестировать работу Uniquifier
6. ⏳ Закоммитить изменения в форк

---

**Автор:** AI Assistant  
**Дата создания:** 2025-12-22

---

## 🎬 ИНФОРМАЦИЯ О ПРОЕКТАХ

### Основной проект (НЕ ТРОГАЕМ!)
- **Timeweb Cloud ID:** 5788751
- **Repository:** Не установлен или приватный
- **Назначение:** Основной production проект
- **Статус:** ⚠️ НЕ ВНОСИМ ИЗМЕНЕНИЯ!
- **Примечание:** Возможно, это проект без git или с локальным git

### Форк проекта (РАБОТАЕМ ЗДЕСЬ!)
- **Timeweb Cloud ID:** 6186087 (Influence Dev)
- **Main Repository:** https://github.com/Synth-Nova/tiktok-uploader-back
- **Backend Submodule:** https://github.com/Synth-Nova/influence1
- **Frontend Submodule:** https://github.com/Synth-Nova/influence2
- **Назначение:** Форк для разработки и тестирования новых фич
- **Статус:** ✅ Все изменения ЗДЕСЬ!

### Важно понимать:
- Основной проект (ID 5788751) и форк (ID 6186087) - **разные серверы Timeweb**
- У них **разные базы кодов** и **разные деплои**
- Форк создан специально для **безопасного тестирования** новых фич
- После успешного тестирования в форке, можно перенести изменения в основной
