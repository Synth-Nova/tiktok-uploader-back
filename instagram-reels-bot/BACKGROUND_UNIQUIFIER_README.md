# 🎬 Background Uniquifier for Speakers

## Проблема

У тебя есть:
- **1 базовое фоновое видео** (аффирмации)
- **8 спикеров** (аккаунтов): Маша, Саша, Петя, и т.д.
- На каждом видео меняется только **текст + ватермарка**

**Риск:** Instagram может детектить дубликаты, даже после уникализатора.

---

## Решение: Уникальные фоны для каждого спикера

`background_uniquifier.py` создаёт **СИЛЬНО отличающиеся** фоновые видео для каждого спикера.

### Что делает для каждого спикера:

| Модификация | Для Маши (idx=0) | Для Саши (idx=1) | Для Пети (idx=2) |
|-------------|------------------|------------------|------------------|
| **Hue shift** | 0° (±10) | 45° (±10) | 90° (±10) |
| **Brightness** | -0.08 → -0.02 | -0.06 → 0.00 | -0.04 → +0.02 |
| **Saturation** | 0.90 → 0.95 | 0.92 → 0.97 | 0.94 → 0.99 |
| **Speed** | 0.96 → 0.99 | 1.01 → 1.04 | 0.96 → 0.99 |
| **Crop** | 0.5% → 2.0% | 0.8% → 2.5% | 1.1% → 3.0% |

**Результат:** Каждый спикер получает **визуально отличающееся** фоновое видео!

---

## 🚀 Использование

### Базовый пример (8 спикеров)

```bash
cd /home/user/webapp/instagram-reels-bot

python3 background_uniquifier.py \
  base_video.mp4 \
  --speakers Маша Саша Петя Оля Вася Катя Дима Лена \
  --output-dir ./unique_backgrounds \
  --analyze
```

**Результат:**
```
./unique_backgrounds/
  ├── Маша_background.mp4    (Hue: 0°, Bright: -0.08)
  ├── Саша_background.mp4    (Hue: 45°, Bright: -0.06)
  ├── Петя_background.mp4    (Hue: 90°, Bright: -0.04)
  ├── Оля_background.mp4     (Hue: 135°, Bright: -0.02)
  ├── Вася_background.mp4    (Hue: 180°, Bright: 0.00)
  ├── Катя_background.mp4    (Hue: 225°, Bright: +0.02)
  ├── Дима_background.mp4    (Hue: 270°, Bright: +0.04)
  └── Лена_background.mp4    (Hue: 315°, Bright: +0.06)
```

---

### Вариации (несколько версий для каждого)

Если нужно несколько версий каждого фона (для 24 языков):

```bash
python3 background_uniquifier.py \
  base_video.mp4 \
  --speakers Маша Саша Петя \
  --output-dir ./backgrounds \
  --variations 2
```

**Результат (3 версии для каждого):**
```
./backgrounds/
  ├── Маша_background_v1.mp4
  ├── Маша_background_v2.mp4
  ├── Маша_background_v3.mp4
  ├── Саша_background_v1.mp4
  ├── Саша_background_v2.mp4
  ├── Саша_background_v3.mp4
  └── ...
```

---

## 🎯 Рекомендуемая схема для твоего случая

### Вариант 1: Один фон на спикера

```bash
# Шаг 1: Создать уникальные фоны для 8 спикеров
python3 background_uniquifier.py \
  affirmations_base.mp4 \
  --speakers Speaker1 Speaker2 Speaker3 Speaker4 Speaker5 Speaker6 Speaker7 Speaker8 \
  --output-dir ./speaker_backgrounds

# Шаг 2: Для каждого языка (24 шт):
# - Взять соответствующий фон спикера
# - Наложить текст аффирмаций (на языке)
# - Добавить ватермарку спикера
# - Прогнать через обычный уникализатор (minimal preset)
```

### Вариант 2: Группы спикеров

Если 8 спикеров — это много, можно сделать **3-4 группы**:

```bash
# Группа 1: Speaker1, Speaker2 (Европа)
python3 background_uniquifier.py base.mp4 \
  --speakers Group1_BG \
  --output-dir ./backgrounds

# Группа 2: Speaker3, Speaker4 (Азия)
python3 background_uniquifier.py base.mp4 \
  --speakers Group2_BG \
  --output-dir ./backgrounds

# Группа 3: Speaker5, Speaker6 (Америка)
python3 background_uniquifier.py base.mp4 \
  --speakers Group3_BG \
  --output-dir ./backgrounds
```

---

## 📊 Что изменяется для каждого спикера

### Критические параметры (максимальная уникализация):

1. **Hue Shift (цветовой тон)** 🌈
   - Спикер 0: 0° (оригинальный цвет)
   - Спикер 1: 45° (оранжевый оттенок)
   - Спикер 2: 90° (зелёный оттенок)
   - Спикер 3: 135° (голубой оттенок)
   - И т.д...

2. **Brightness/Contrast/Saturation** ☀️
   - Каждый спикер — свой уровень яркости
   - Разные диапазоны насыщенности

3. **Speed** ⏱️
   - Чётные спикеры: 96-99% (медленнее)
   - Нечётные спикеры: 101-104% (быстрее)

4. **Color Shift (RGB каналы)** 🎨
   - Уникальный сдвиг цветовых каналов

5. **Invisible Watermark** 💧
   - Разная прозрачность для каждого

---

## 🧪 Тестирование

### Проверить, насколько отличаются видео:

```bash
python3 background_uniquifier.py \
  base.mp4 \
  --speakers Маша Саша \
  --output-dir ./test \
  --analyze
```

**Вывод покажет:**
```
📊 Comparison 1: Маша_background.mp4 vs Саша_background.mp4
  Hashes different: ✅ Yes
  Size ratio: 98.5%
  Size diff: -154,239 bytes
```

---

## 💡 Рекомендации

### Для 24 языков + 8 спикеров:

**Схема А (Безопасная):**
```
8 уникальных фонов (по одному на спикера)
  ↓
Для каждого языка:
  - Взять фон спикера
  - Наложить текст (язык)
  - Добавить ватермарку
  - Прогнать через уникализатор (minimal)
  ↓
Итого: 24 языка × 8 спикеров = 192 видео
```

**Схема Б (Максимальная уникальность):**
```
3-4 базовых фона (для групп спикеров)
  ↓
Для каждой комбинации (фон + спикер + язык):
  - Генерировать уникальный фон
  - Наложить текст
  - Прогнать через уникализатор (balanced)
  ↓
Итого: более безопасно, но дольше обрабатывать
```

### Оптимальный выбор:

🎯 **Рекомендую Схему А** + `minimal` уникализатор для финальных видео

**Почему:**
- 8 сильно отличающихся фонов уже дают огромную вариативность
- Наложение текста + ватермарка добавляют уникальности
- `minimal` уникализатор на финале — страховка от дубликатов
- Быстрая обработка

---

## 🔧 Интеграция с твоим существующим уникализатором

```python
from background_uniquifier import BackgroundUniquifier
from src.tools.video_uniquifier import VideoUniquifier

# 1. Создаём уникальные фоны для спикеров
bg_uniq = BackgroundUniquifier()
bg_results = bg_uniq.create_backgrounds_for_speakers(
    base_video_path="base_affirmations.mp4",
    speakers=["Speaker1", "Speaker2", "Speaker3", "Speaker4",
              "Speaker5", "Speaker6", "Speaker7", "Speaker8"],
    output_dir="./speaker_backgrounds"
)

# 2. Для каждого языка и спикера:
uniq = VideoUniquifier()

for speaker_bg in bg_results:
    speaker_name = speaker_bg['speaker']
    bg_path = speaker_bg['path']
    
    for language in ["en", "ru", "es", "fr", ...]:  # 24 языка
        # TODO: Наложить текст аффирмаций на язык
        video_with_text = add_text_overlay(bg_path, language)
        
        # TODO: Добавить ватермарку спикера
        video_with_watermark = add_watermark(video_with_text, speaker_name)
        
        # Финальная уникализация (minimal)
        final_video, info = uniq.uniquify(
            video_with_watermark,
            output_path=f"./final/{speaker_name}_{language}.mp4",
            preset="minimal"
        )
```

---

## ❓ FAQ

**Q: Насколько визуально отличаются видео?**
A: Hue shift на 45° заметен, но выглядит естественно. Если переживаешь — используй `--analyze` для проверки.

**Q: Можно ли сделать ещё более незаметные изменения?**
A: Да, отредактируй функцию `create_speaker_preset()` и уменьши диапазоны.

**Q: Зачем нужен `--variations`?**
A: Если хочешь несколько версий каждого фона (например, для A/B тестов).

**Q: Сколько времени занимает обработка?**
A: ~30-60 секунд на одно видео (зависит от длины и разрешения).

---

## 📞 Что дальше?

1. **Попробуй скрипт** на своём фоновом видео
2. **Проверь результаты** через `--analyze`
3. **Если устраивает** — интегрируй в свой workflow
4. **Если нужны доработки** — пиши, настроим!

