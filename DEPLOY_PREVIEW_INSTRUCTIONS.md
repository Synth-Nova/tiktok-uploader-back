# 🎬 Инструкция по деплою превью видео шотов

## Что добавлено:
✅ Видео-превью для каждого шота (160x120px)
✅ Подробная информация: разрешение, FPS, длительность
✅ Endpoint для раздачи временных файлов: `/api/montage-v2/preview/<filename>`

## Файлы для деплоя:
- **Frontend**: `/tmp/frontend-preview.tar.gz`
- **Backend**: `/tmp/backend-preview.tar.gz`

## Команды для деплоя на сервере (217.198.12.144):

### 1. Загрузить архивы на сервер
```bash
# Скачайте архивы из sandbox:
# - /tmp/frontend-preview.tar.gz
# - /tmp/backend-preview.tar.gz
# И загрузите их в /tmp/ на сервере
```

### 2. Деплой фронтенда
```bash
ssh root@217.198.12.144
cd /opt/influence-frontend
rm -rf build_backup
mv build build_backup 2>/dev/null || true
mkdir -p build
tar -xzf /tmp/frontend-preview.tar.gz -C build/
rm /tmp/frontend-preview.tar.gz
```

### 3. Деплой бэкенда
```bash
cd /opt
tar -xzf /tmp/backend-preview.tar.gz -C /opt/
rm -rf video-editor
mv video-editor-module video-editor
rm /tmp/backend-preview.tar.gz
```

### 4. Перезапуск сервисов
```bash
pm2 restart video-editor-api
nginx -t && systemctl reload nginx
pm2 status
```

## Проверка работы:
1. Откройте: https://upl.synthnova.me/video-editor-v2
2. Очистите кэш браузера: Ctrl + Shift + R
3. Войдите: admin / rewfdsvcx5
4. Загрузите видео для анализа
5. **Теперь вы увидите превью каждого шота!**

## Что изменилось в UI:
- Каждый шот теперь показывает видео-превью слева
- Информация о файле справа от превью
- Улучшенная визуальная структура карточки шота

